"""Local performance-benchmark suite for boto3-s3 (see docs/benchmark.md).

Run with ``uv run python -m benchmarks run`` from the repository root. Not a
test suite: pytest never collects this package, and nothing here ships in
either distributed package.
"""
